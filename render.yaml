services:
  - type: web
    name: sports-rules-mcp
    runtime: python
    buildCommand: pip install -e . && pip install uvicorn
    startCommand: uvicorn http_server:app --host 0.0.0.0 --port $PORT
    plan: free
